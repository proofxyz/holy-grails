// Directories
export const ASSET_DIR = './assets';
const TMP_DIR = '/tmp';

export const SOURCE_DIR = `${ASSET_DIR}/source`;
export const PROCESSING_DIR = `${TMP_DIR}/grails`;
export const PROCESSED_DIR = `${ASSET_DIR}/source-processed`;
export const CLEAN_DIR = `${ASSET_DIR}/clean`;

export const EXIFTOOL_SUFFIX = '_original';

// Config
export const DEBUG_MODE = false;
